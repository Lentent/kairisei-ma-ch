// Package cpk resolves the client's logical audio names to physical files,
// revisions and wire manifests. HTTP serving and CDN export consume the same
// inventory; this package does not own accounts, routes or database state.
package cpk
